class CleanInputs:
    @staticmethod
    def strings(input):
        if (input):
            return input
        else:
            return ""

    @staticmethod
    def instances(instances):
        if (instances):
            return instances
        else:
            return []

    @staticmethod
    def instance(instance):
        if (instance):
            return instance
        else:
            return ""